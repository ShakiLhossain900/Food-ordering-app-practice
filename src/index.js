////practicing code watching video agian and try to find out the problem 
///there is a problem in code when i click in the cart button that button shoud work but element can't add to the header cart button
//that i start agian recheck the code again what is the problem in the code
import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
